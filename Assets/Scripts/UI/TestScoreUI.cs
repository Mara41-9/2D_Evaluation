using UnityEngine;
using UnityEngine.UI;

public class TestScoreUI : MonoBehaviour
{
    //[SerializeField] private Text Text_PlayerExp;
    
    //public void AddPlayerExp(int addExp)
    //{
    //    // 플레이어 경험치를 증가시키는 부분
    //    GameManager.Instance.IncreasePlayerExp(addExp);

    //    // 증가된 현재 총 경험치를 가져와서 변수에 저장
    //    int currentExp = GameManager.Instance.GetPlayerExp();
    //    // Text_PlayerExp의 텍스트 변경
    //    Text_PlayerExp.text = $"플레이어 Exp: {currentExp}";

    //    // 100 단위일때마다
    //    if ((currentExp % 100) == 0)
    //    {
    //        Debug.LogWarning("저장 시도!");
    //        // 현재 플레이어 데이터를 JSON 파일로 저장 요청
    //        GameManager.Instance.SaveData();
    //        Debug.LogWarning($"저장된 플레이어 Exp: {currentExp}");
    //    }
    //}

}
